{{- if .Values.sidecar.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "comms-service.fullname" . }}-sidecar
  labels:
    {{- include "comms-service.labels" . | nindent 4 }}
    component: sidecar
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "comms-service.selectorLabels" . | nindent 4 }}
{{- end }}