{{- if .Values.sidecar.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "client-service.fullname" . }}-sidecar
  labels:
    {{- include "client-service.labels" . | nindent 4 }}
    component: sidecar
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "client-service.selectorLabels" . | nindent 4 }}
{{- end }}